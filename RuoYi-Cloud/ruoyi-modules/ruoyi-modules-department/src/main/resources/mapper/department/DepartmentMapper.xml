<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.department.mapper.DepartmentMapper">

    <resultMap type="com.ruoyi.department.domain.Department" id="DepartmentResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="createdAt"    column="created_at"    />
        <result property="isDeleted"    column="is_deleted"    />
        <result property="deletedAt"    column="deleted_at"    />
        <result property="overview"    column="overview"    />
    </resultMap>

    <sql id="selectDepartmentVo">
        select id, name, created_at, is_deleted, deleted_at from department
    </sql>

    <select id="selectDepartmentList" resultMap="DepartmentResult">
        <include refid="selectDepartmentVo"/>
        <where>
            <choose>
                <when test="params.includeDeleted != null and params.includeDeleted == 'true'">
                    and is_deleted = 1
                </when>
                <otherwise>
                    and is_deleted = 0
                </otherwise>
            </choose>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </select>

    <select id="selectDepartmentWithIntroList" resultMap="DepartmentResult">
        SELECT d.*, di.overview
        FROM department d
        LEFT JOIN department_intro di ON d.id = di.dept_id AND di.is_active = 1
        <where>
            <choose>
                <when test="params.includeDeleted != null and params.includeDeleted == 'true'">
                    and d.is_deleted = 1
                </when>
                <otherwise>
                    and d.is_deleted = 0
                </otherwise>
            </choose>
            <if test="name != null and name != ''">
                AND d.name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </select>

</mapper>
