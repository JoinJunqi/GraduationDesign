<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.patient.mapper.PatientMapper">
    
    <resultMap type="com.ruoyi.patient.domain.Patient" id="PatientResult">
        <result property="id"    column="id"    />
        <result property="username"    column="username"    />
        <result property="passwordHash"    column="password_hash"    />
        <result property="name"    column="name"    />
        <result property="phone"    column="phone"    />
        <result property="idCard"    column="id_card"    />
        <result property="avatar"    column="avatar"    />
        <result property="isActive"    column="is_active"    />
        <result property="createdAt"    column="created_at"    />
        <result property="isDeleted"    column="is_deleted"    />
        <result property="deletedAt"    column="deleted_at"    />
    </resultMap>

    <sql id="selectPatientVo">
        select id, username, password_hash, name, phone, id_card, avatar, is_active, created_at, is_deleted, deleted_at
        from patient
    </sql>

    <select id="selectPatientList" parameterType="com.ruoyi.patient.domain.Patient" resultMap="PatientResult">
        <include refid="selectPatientVo"/>
        <where>  
            <choose>
                <when test="params.includeDeleted != null and params.includeDeleted == 'true'">
                    and is_deleted = 1
                </when>
                <otherwise>
                    and is_deleted = 0
                </otherwise>
            </choose>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="username != null  and username != ''"> and username like concat('%', #{username}, '%')</if>
            <if test="phone != null  and phone != ''"> and phone like concat('%', #{phone}, '%')</if>
            <if test="idCard != null  and idCard != ''"> and id_card = #{idCard}</if>
            <if test="isActive != null "> and is_active = #{isActive}</if>
        </where>
    </select>
    
</mapper>
